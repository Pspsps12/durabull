{
  "name": "@durabull/desktop",
  "productName": "Durabull",
  "version": "1.2.0",
  "private": true,
  "description": "Durabull native desktop shell",
  "author": "Durabull",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/durabullhq/durabull.git"
  },
  "main": "dist/main.js",
  "scripts": {
    "build": "bun run build:main && bun run stage",
    "build:main": "bun build src/main.ts --target=node --format=cjs --outfile=dist/main.js --external electron",
    "stage": "bun scripts/stage.ts",
    "start": "bun run build && bun scripts/start.ts",
    "dist": "bun run build && electron-builder --config electron-builder.config.cjs --publish never",
    "dist:publish": "bun run build && electron-builder --config electron-builder.config.cjs --publish always -c.publish.provider=github -c.publish.owner=durabullhq -c.publish.repo=durabull -c.publish.channel=latest",
    "dist:dir": "bun run build && electron-builder --config electron-builder.config.cjs --dir --publish never",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "biome lint ./src ./scripts",
    "lint:fix": "biome lint --write ./src ./scripts",
    "format": "biome format --write ./src ./scripts",
    "format:check": "biome format ./src ./scripts"
  },
  "devDependencies": {
    "@durabull/api": "workspace:*",
    "@durabull/web": "workspace:*",
    "@types/node": "^22.10.5",
    "electron": "^41.0.2",
    "electron-builder": "^26.8.1",
    "typescript": "^5.7.2"
  }
}
